<h1>Admin</h1>

<%= button_to 'Create User', admin_new_path, data:{ turbo:false }, method: :get %>
<%= button_to 'See All Traders', admin_trader_list_path, data:{ turbo:false }, method: :get %>

<table>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %>
      <td><%= user.approved %>
    </tr>
  <% end %>
</table>